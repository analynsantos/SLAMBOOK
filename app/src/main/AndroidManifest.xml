<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.slambook">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/slambook_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/slambook_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SLAMBOOK">
        <activity android:name=".Activity6_EditEntry"></activity>
        <activity android:name=".Activity4_IndividualEntry" />
        <activity android:name=".RecyclerviewTab" />
        <activity android:name=".Activity5_AddEntry" />
        <activity android:name=".Activity3_EntryList" />
        <activity android:name=".Activity2_Register"/>
        <activity
            android:name=".Activity1_LogIn"
            android:noHistory="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

</manifest>